generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL_MONGO")
}

model Product {
  id       String   @id @default(auto()) @map("_id") @db.ObjectId
  product  String
  quantity Int
  price    Int
  provide  String   @unique
  date     DateTime @default(now())
}
